# the build context is tools/ only — the source tree is bind-mounted at run time,
# never copied — but keep these out of any context by accident.
src-tauri/target
node_modules
dist
out
artifacts
.git
.secrets
*.key
*.key.password
*.pfx
*.p12
